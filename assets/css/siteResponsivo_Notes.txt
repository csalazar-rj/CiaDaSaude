:root {
  --ink: #17312d;
  --muted: #65746f;
  --brand: #d18c25;
  --brand-dark: #c29234;
  --mint: #dff5ed;
  --leaf: #cb9a37;
  --cream: #fbfaf5;
  --line: #d9e7e1;
  --white: #fff;
  --warn: #f3a23a;
  --shadow: 0 18px 50px rgba(23, 49, 45, .12);
  --radius-pill: 999px;
}

* { box-sizing: border-box; }

html { scroll-behavior: smooth; }

body {
  margin: 0;
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  color: var(--ink);
  background: var(--cream);
  line-height: 1.55;
}

a { color: inherit; text-decoration: none; }

img { max-width: 100%; display: block; }

.topbar {
  position: sticky;
  top: 0;
  z-index: 10;
  background: rgba(251, 250, 245, .94);
  border-bottom: 1px solid var(--line);
  backdrop-filter: blur(14px);
}

.nav {
  max-width: 1180px;
  margin: 0 auto;
  padding: 14px 24px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18px;
}

.nav-toggle {
  display: none;
  align-items: center;
  justify-content: center;
  width: 44px;
  height: 44px;
  border: 1px solid var(--line);
  border-radius: 6px;
  background: var(--white);
  color: var(--ink);
  cursor: pointer;
}

.nav-toggle span,
.nav-toggle::before,
.nav-toggle::after {
  content: "";
  display: block;
  width: 20px;
  height: 2px;
  border-radius: 999px;
  background: currentColor;
  transition: transform .18s ease, opacity .18s ease;
}

.nav-toggle {
  flex-direction: column;
  gap: 5px;
}

.nav.menu-open .nav-toggle::before { transform: translateY(7px) rotate(45deg); }
.nav.menu-open .nav-toggle span { opacity: 0; }
.nav.menu-open .nav-toggle::after { transform: translateY(-7px) rotate(-45deg); }

.brand {
  display: flex;
  align-items: center;
  gap: 10px;
  font-weight: 850;
  letter-spacing: 0;
}

.brand-mark {
  width: 42px;
  height: 42px;
  border-radius: 8px;
  background: linear-gradient(135deg, var(--brand), var(--leaf));
  color: var(--white);
  display: grid;
  place-items: center;
  font-weight: 900;
}

.menu {
  display: flex;
  align-items: center;
  gap: 4px;
  flex-wrap: wrap;
  justify-content: flex-end;
}

.menu a, .submenu-button {
  padding: 10px 12px;
  border-radius: 6px;
  font-weight: 700;
  font-size: 14px;
  color: var(--ink);
  border: 0;
  background: transparent;
  cursor: pointer;
}

.menu a:focus-visible, .submenu-button:focus-visible, .nav-toggle:focus-visible {
  outline: 3px solid rgba(15, 143, 120, .32);
  outline-offset: 2px;
}

.menu a:hover, .submenu:hover .submenu-button { background: var(--mint); }

.nav-whatsapp {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 42px;
  padding: 10px 18px;
  border-radius: var(--radius-pill);
  background: var(--brand);
  color: var(--white);
  font-size: 14px;
  font-weight: 800;
  white-space: nowrap;
  box-shadow: 0 10px 22px -14px rgba(209, 140, 37, .75);
}

.nav-whatsapp:hover,
.nav-whatsapp:focus-visible {
  background: var(--brand-dark);
  color: var(--white);
  outline-color: rgba(209, 140, 37, .36);
}

.whatsapp-float {
  position: fixed;
  right: 22px;
  bottom: 22px;
  z-index: 120;
  width: 58px;
  height: 58px;
  display: grid;
  place-items: center;
  border-radius: 50%;
  background: var(--brand);
  color: var(--white);
  box-shadow: 0 18px 38px rgba(23, 49, 45, .22);
  transition: transform .18s ease, background .18s ease, box-shadow .18s ease;
}

.whatsapp-float:hover,
.whatsapp-float:focus-visible {
  background: var(--brand-dark);
  transform: translateY(-2px);
  box-shadow: 0 22px 44px rgba(23, 49, 45, .28);
}

.whatsapp-float svg {
  width: 32px;
  height: 32px;
  fill: currentColor;
}

.submenu { position: relative; }

.submenu-panel {
  display: none;
  position: absolute;
  top: 42px;
  left: 0;
  min-width: 210px;
  padding: 8px;
  background: var(--white);
  border: 1px solid var(--line);
  box-shadow: var(--shadow);
  border-radius: 8px;
}

.submenu:hover .submenu-panel { display: grid; }

.submenu-panel a { border-radius: 6px; }

.hero {
  background:
    linear-gradient(90deg, rgba(208, 108, 31, 0.9), rgba(172, 110, 24, 0.72)),
    url("hero-lab.svg") center/cover no-repeat;
  color: var(--white);
}

.hero-inner {
  max-width: 1180px;
  margin: 0 auto;
  padding: 78px 24px 56px;
  display: grid;
  grid-template-columns: minmax(0, 1fr) 440px;
  gap: 34px;
  align-items: center;
}

.eyebrow {
  /* color: var(--leaf); */
  color: orange;
  text-transform: uppercase;
  font-size: 12px;
  font-weight: 900;
  letter-spacing: .12em;
}

.hero .eyebrow { color: #ffe7bb; }

h1, h2, h3 { line-height: 1.1; margin: 0 0 14px; letter-spacing: 0; }

h1 { font-size: clamp(40px, 6vw, 72px); max-width: 700px; }
h2 { font-size: clamp(28px, 4vw, 44px); }
h3 { font-size: 22px; }

p { margin: 0 0 16px; color: var(--muted); }
.hero p { color: rgba(255,255,255,.88); font-size: 19px; max-width: 640px; }

.actions { display: flex; gap: 12px; flex-wrap: wrap; margin-top: 24px; }

.monica-hero {
  padding: 64px 0 40px;
  background:
    radial-gradient(ellipse 900px 500px at 85% -10%, rgba(209, 140, 37, .14), transparent 60%),
    #f2fbf8;
  color: var(--ink);
  overflow: hidden;
}

.monica-container {
  width: 100%;
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 28px;
}

.monica-hero-grid {
  display: grid;
  grid-template-columns: 1.05fr .85fr;
  gap: 50px;
  align-items: center;
}

.monica-eyebrow {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 22px;
  padding: 8px 18px;
  border-radius: var(--radius-pill);
  background: #fff1e4;
  color: var(--brand);
  font-size: 12.5px;
  font-weight: 700;
  letter-spacing: .14em;
  text-transform: uppercase;
}

.monica-eyebrow::before {
  content: "";
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--brand);
  flex-shrink: 0;
}

.monica-hero h1 {
  margin-bottom: 22px;
  color: var(--ink);
  font-family: Georgia, "Times New Roman", serif;
  font-size: clamp(38px, 5.2vw, 60px);
  font-weight: 500;
  line-height: 1.12;
}

.monica-hero h1 em {
  color: var(--brand);
  font-style: italic;
  font-weight: 500;
}

.monica-hero .monica-lead {
  max-width: 56ch;
  margin-bottom: 32px;
  color: #2a4d48;
  font-size: 18px;
}

.monica-hero-ctas {
  display: flex;
  gap: 14px;
  flex-wrap: wrap;
  margin-bottom: 44px;
}

.monica-btn {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  padding: 15px 30px;
  border: 1.5px solid transparent;
  border-radius: var(--radius-pill);
  font-size: 15px;
  font-weight: 600;
  cursor: pointer;
  white-space: nowrap;
  transition: transform .25s ease, box-shadow .25s ease, background .25s ease, color .25s ease;
}

.monica-btn svg {
  width: 17px;
  height: 17px;
  flex-shrink: 0;
}

.monica-btn-primary {
  background: var(--brand);
  color: var(--white);
  box-shadow: 0 14px 30px -12px rgba(209, 140, 37, .58);
}

.monica-btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 18px 36px -12px rgba(209, 140, 37, .68);
}

.monica-btn-outline {
  background: transparent;
  border-color: rgba(15, 46, 44, .1);
  color: var(--ink);
}

.monica-btn-outline:hover {
  border-color: var(--ink);
  transform: translateY(-2px);
}

.monica-hero-stats {
  display: flex;
  gap: 34px;
  flex-wrap: wrap;
}

.monica-hero-stat .num {
  display: block;
  color: var(--brand);
  font-family: Georgia, "Times New Roman", serif;
  font-size: 30px;
}

.monica-hero-stat .label {
  color: #2a4d48;
  font-size: 13px;
  font-weight: 600;
}

.monica-capsule-wrap {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 480px;
}

.monica-capsule {
  position: relative;
  width: 190px;
  height: 420px;
  overflow: hidden;
  border: 1px solid rgba(15, 46, 44, .06);
  border-radius: 100px;
  background: var(--white);
  box-shadow: 0 40px 70px -25px rgba(15, 46, 44, .45);
}

.monica-capsule .half-top {
  position: absolute;
  inset: 0 0 auto;
  height: 52%;
  background: linear-gradient(160deg, #163b37, #0f2e2c);
}

.monica-capsule .half-bottom {
  position: absolute;
  inset: auto 0 0;
  height: 50%;
  background: linear-gradient(160deg, #ffa662, var(--brand));
}

.monica-capsule .shine {
  position: absolute;
  top: 8%;
  left: 16%;
  width: 26px;
  height: 64%;
  border-radius: 20px;
  background: rgba(255, 255, 255, .35);
  filter: blur(1px);
}

.monica-capsule .granule {
  position: absolute;
  z-index: 1;
  border-radius: 50%;
  background: var(--white);
  opacity: .85;
}

.monica-orbit-ring {
  position: absolute;
  border: 1.5px dashed rgba(15, 46, 44, .18);
  border-radius: 50%;
}

.monica-orbit-1 {
  top: 10%;
  left: 50%;
  width: 340px;
  height: 340px;
  transform: translateX(-50%);
  animation: monica-spin 40s linear infinite;
}

.monica-orbit-2 {
  top: 2%;
  left: 50%;
  width: 430px;
  height: 430px;
  transform: translateX(-50%);
  animation: monica-spin 60s linear infinite reverse;
}

.monica-orbit-dot {
  position: absolute;
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: var(--brand);
}

.monica-orbit-1 .monica-orbit-dot {
  top: -5px;
  left: 50%;
}

.monica-orbit-2 .monica-orbit-dot {
  right: 8%;
  bottom: 6%;
  width: 8px;
  height: 8px;
  background: var(--ink);
}

@keyframes monica-spin {
  to { transform: translateX(-50%) rotate(360deg); }
}

@keyframes float-up {
  0% {
    transform: translateY(0);
    opacity: 0;
  }

  15% {
    opacity: 1;
  }

  100% {
    transform: translateY(-160px);
    opacity: 0;
  }
}

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 44px;
  padding: 12px 18px;
  border-radius: 6px;
  border: 1px solid transparent;
  background: var(--brand);
  color: var(--white);
  font-weight: 850;
  cursor: pointer;
}

.btn:hover { background: var(--brand-dark); }

.btn.secondary {
  background: var(--white);
  color: var(--brand-dark);
  border-color: var(--line);
}

.btn.ghost {
  background: transparent;
  border-color: rgba(255,255,255,.45);
  color: var(--white);
}

.quote-box {
  background: var(--white);
  color: var(--ink);
  padding: 22px;
  border-radius: 8px;
  box-shadow: var(--shadow);
}

.quote-box p { font-size: 14px; color: var(--muted); }

.form-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 12px;
}

.field { display: grid; gap: 6px; }
.field.full { grid-column: 1 / -1; }

label { font-size: 13px; font-weight: 850; color: var(--ink); }

input, select, textarea {
  width: 100%;
  min-height: 44px;
  border: 1px solid var(--line);
  border-radius: 6px;
  padding: 10px 12px;
  font: inherit;
  color: var(--ink);
  background: var(--white);
}

textarea { min-height: 118px; resize: vertical; }

.section {
  max-width: 1180px;
  margin: 0 auto;
  padding: 70px 24px;
}

.section.band {
  max-width: none;
  background: var(--white);
  border-block: 1px solid var(--line);
}

.section.band > .inner { max-width: 1180px; margin: 0 auto; }

.section-head {
  display: flex;
  align-items: end;
  justify-content: space-between;
  gap: 20px;
  margin-bottom: 28px;
}

.section-head p { max-width: 620px; }

.product-grid, .card-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 18px;
}

.product-card, .info-card, .post-card {
  background: var(--white);
  border: 1px solid var(--line);
  border-radius: 8px;
  overflow: hidden;
  box-shadow: 0 12px 35px rgba(23, 49, 45, .08);
}

.product-card:hover, .post-card:hover { transform: translateY(-2px); transition: .18s ease; }

.product-card img { width: 100%; height: 180px; object-fit: cover; background: var(--mint); }

.product-card .pad, .info-card, .post-card { padding: 18px; }

.wide-link {
  display: block;
  border-radius: 8px;
  overflow: hidden;
  border: 1px solid var(--line);
  box-shadow: var(--shadow);
  background: var(--white);
}

.wide-link img { width: 100%; min-height: 310px; object-fit: cover; }

main > img {
  width: 100%;
  max-height: 520px;
  object-fit: cover;
}

.split {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 26px;
  align-items: center;
}

.page-hero {
  background: linear-gradient(120deg, var(--mint), var(--white));
  border-bottom: 1px solid var(--line);
}

.page-hero .section { padding-block: 54px; }

.content {
  max-width: 900px;
  margin: 0 auto;
  padding: 58px 24px;
}

.content.wide { max-width: 1180px; }

.editable-footer {
  background: #102722;
  color: var(--white);
}

.footer-grid {
  max-width: 1180px;
  margin: 0 auto;
  padding: 38px 24px;
  display: grid;
  grid-template-columns: 1.2fr 1fr 1fr;
  gap: 24px;
}

.editable-footer p, .editable-footer a { color: rgba(255,255,255,.78); }

.footer-note {
  border-top: 1px solid rgba(255,255,255,.12);
  padding: 14px 24px;
  text-align: center;
  color: rgba(255,255,255,.58);
  font-size: 13px;
}

.notice {
  padding: 14px 16px;
  border: 1px solid var(--line);
  background: var(--mint);
  border-radius: 8px;
  color: var(--ink);
  margin-bottom: 18px;
}

.login-shell {
  max-width: 520px;
  margin: 0 auto;
  padding: 58px 24px;
}

.checkboxes { display: grid; gap: 10px; margin: 12px 0; }
.checkboxes label { display: flex; align-items: start; gap: 10px; font-weight: 650; }
.checkboxes input { width: auto; min-height: auto; margin-top: 4px; }

.hidden { display: none !important; }

/* *********************************
AI source - Begin
********************************* */

header {
  position: sticky;
  top: 0;
  z-index: 100;
  padding: 18px 20px;
}

.nav-pill {
  max-width: 1200px;
  margin: 0 auto;
  background: rgba(255, 255, 255, 0.82);
  backdrop-filter: blur(14px);
  border: 1px solid rgba(15, 46, 44, 0.08);
  border-radius: var(--radius-pill);
  padding: 10px 12px 10px 22px;
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
  align-items: center;
  gap: 16px;
  box-shadow: 0 10px 30px -18px rgba(15, 46, 44, 0.35);
}

.nav-pill .nav {
  max-width: none;
  margin: 0;
  padding: 0;
  justify-content: center;
}

.nav-pill > a {
  justify-self: start;
}

.nav-pill .menu {
  justify-content: center;
}

.logo-img {
  height: 34px;
  width: auto;
}

.nav-cta {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 8px;
}


/* *********************************
AI source - End
********************************* */


@media (max-width: 900px) {
  .hero-inner, .split, .footer-grid { grid-template-columns: 1fr; }
  .hero-inner { padding-top: 44px; }
  .monica-hero-grid { grid-template-columns: 1fr; }
  .monica-capsule-wrap {
    height: 390px;
    order: -1;
  }
  .monica-capsule {
    width: 150px;
    height: 330px;
  }
  .monica-orbit-1 {
    width: 270px;
    height: 270px;
  }
  .monica-orbit-2 {
    width: 340px;
    height: 340px;
  }
  .product-grid, .card-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .quote-box { max-width: 620px; }
}

@media (max-width: 760px) {
  body { overflow-x: hidden; }

  .topbar { position: sticky; }

  .nav-pill {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto 44px;
    gap: 10px;
    padding: 10px 12px 10px 18px;
  }

  .nav-pill > a {
    grid-column: 1;
    grid-row: 1;
    min-width: 0;
  }

  .nav-pill .nav {
    display: contents;
  }

  .nav-pill .nav-cta {
    grid-column: 2;
    grid-row: 1;
  }

  .nav-pill .nav-toggle {
    grid-column: 3;
    grid-row: 1;
  }

  .nav-pill .menu {
    grid-column: 1 / -1;
    grid-row: 2;
    justify-content: flex-start;
  }

  .nav {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 44px;
    align-items: center;
    gap: 12px;
    padding: 10px 16px;
  }

  .brand {
    min-width: 0;
    font-size: 16px;
  }

  .brand-mark {
    width: 38px;
    height: 38px;
    flex: 0 0 auto;
  }

  .nav-toggle { display: flex; }

  .menu {
    grid-column: 1 / -1;
    display: none;
    width: 100%;
    padding: 10px 0 4px;
    border-top: 1px solid var(--line);
    align-items: stretch;
    justify-content: flex-start;
    flex-direction: column;
    gap: 6px;
  }

  .nav.menu-open .menu { display: flex; }

  .menu a, .submenu-button {
    width: 100%;
    min-height: 44px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 11px 12px;
    background: var(--white);
    border: 1px solid var(--line);
  }

  .submenu { width: 100%; }

  .submenu-button::after {
    content: "+";
    font-size: 18px;
    line-height: 1;
  }

  .submenu-panel {
    position: static;
    display: grid;
    min-width: 0;
    width: 100%;
    margin-top: 6px;
    padding: 0 0 0 12px;
    background: transparent;
    border: 0;
    box-shadow: none;
  }

  .submenu-panel a {
    background: rgba(255,255,255,.72);
  }

  .hero-inner {
    gap: 22px;
    padding-block: 34px 38px;
  }

  h1 { font-size: clamp(34px, 11vw, 48px); }
  h2 { font-size: clamp(26px, 8vw, 36px); }

  .hero p { font-size: 17px; }

  .actions, .quote-box .btn {
    width: 100%;
  }

  .actions .btn, .quote-box .btn {
    width: 100%;
  }

  .monica-container {
    padding-inline: 18px;
  }

  .monica-hero {
    padding-bottom: 36px;
  }

  .monica-hero h1 {
    font-size: clamp(34px, 11vw, 46px);
  }

  .monica-hero .monica-lead {
    font-size: 17px;
  }

  .monica-hero-ctas {
    margin-bottom: 34px;
  }

  .monica-btn {
    width: 100%;
    justify-content: center;
  }

  .monica-hero-stats {
    gap: 22px;
  }

  .form-grid, .product-grid, .card-grid { grid-template-columns: 1fr; }
  .section-head { display: block; }

  .section, .content, .login-shell {
    padding-inline: 18px;
    padding-block: 42px;
  }

  .page-hero .section { padding-block: 36px; }

  .hero-inner, .section { padding-inline: 18px; }

  .quote-box {
    width: 100%;
    padding: 18px;
  }

  .product-card img { height: 168px; }

  .wide-link img {
    min-height: 210px;
  }

  main > img {
    min-height: 230px;
    max-height: 360px;
  }

  .footer-grid {
    padding: 30px 18px;
    gap: 12px;
  }

  .whatsapp-float {
    right: 16px;
    bottom: 16px;
    width: 54px;
    height: 54px;
  }

  .whatsapp-float svg {
    width: 30px;
    height: 30px;
  }
}

@media (max-width: 380px) {
  .brand { font-size: 15px; }
  .nav { padding-inline: 12px; }
  h1 { font-size: 32px; }
  .menu a, .submenu-button { font-size: 13px; }
  .nav-whatsapp {
    min-height: 40px;
    padding-inline: 14px;
    font-size: 13px;
  }
}
